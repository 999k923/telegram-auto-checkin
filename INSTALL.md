# 📦 一键部署教程

> 适用于完全不懂技术的小白用户

---

## 🎯 部署前准备

### 1. 准备一台服务器

**推荐配置：**
- 系统：Ubuntu 20.04 / 22.04 或 Debian 11
- 内存：512MB 以上
- 硬盘：1GB 以上
- 网络：可以访问 Telegram

**常见云服务商：**
- 腾讯云轻量应用服务器
- 阿里云ECS
- AWS EC2
- Vultr / DigitalOcean

### 2. 获取 Telegram API 凭证

**步骤：**

1. 在浏览器打开：https://my.telegram.org
2. 用你的手机号登录（就是平时用 Telegram 的手机号）
3. 点击 **"API development tools"**（API 开发工具）
4. 随便填写应用信息（App title 和 Short name 随意填）
5. 点击 **"Create application"**（创建应用）
6. 记下 **API_ID**（一串数字）和 **API_HASH**（一串字母数字）

> ⚠️ 保密这两个值，不要泄露给他人！

---

## 🚀 方式一：从 GitHub 一键部署（推荐）

### 步骤 1：连接服务器

**Windows 用户：**
- 下载 **Xshell** 或 **PuTTY**
- 输入服务器 IP 地址、用户名、密码
- 点击连接

**Mac/Linux 用户：**
```bash
ssh root@你的服务器IP
# 或者
ssh 用户名@你的服务器IP
```

### 步骤 2：下载并运行安装脚本

复制以下命令，粘贴到终端（右键粘贴），然后按回车：

```bash
git clone https://github.com/你的用户名/telegram-auto-checkin.git
cd telegram-auto-checkin
sudo bash install.sh
```

> 💡 将 `你的用户名` 替换为你的 GitHub 用户名

### 步骤 3：按提示输入信息

安装脚本会自动引导你完成以下步骤：

1. **输入 API_ID**（纯数字，如：12345678）
2. **输入 API_HASH**（32位字符，如：abcd1234...）
3. **输入手机号**（格式：+8613800138000）
4. **配置签到目标**：
   - 输入群组/机器人名称
   - 选择签到方式（按钮或命令）
   - 可以添加多个目标

### 步骤 4：完成首次登录

```bash
cd ~/telegram-auto-checkin
source venv/bin/activate
python test_login.py
```

会提示输入验证码，打开 Telegram 查看验证码并输入。

### 步骤 5：测试签到

```bash
python manual_checkin.py
```

如果看到 ✅ 签到成功，说明配置正确！

### 步骤 6：启动自动签到

**推荐方式（开机自启）：**
```bash
sudo ./setup_service.sh
sudo systemctl start telegram-auto-checkin
sudo systemctl enable telegram-auto-checkin
```

**检查运行状态：**
```bash
sudo systemctl status telegram-auto-checkin
```

---

## 📥 方式二：手动上传部署

### 步骤 1：打包项目文件

**在本地电脑：**

1. 将整个项目文件夹压缩成 `telegram-auto-checkin.zip`
2. 使用 **WinSCP** (Windows) 或 **FileZilla** (全平台) 上传到服务器

**上传到服务器的目录：**
```
/root/telegram-auto-checkin/
```

### 步骤 2：解压并安装

**SSH 连接到服务器后：**

```bash
cd ~
unzip telegram-auto-checkin.zip
cd telegram-auto-checkin
sudo bash install.sh
```

后续步骤同方式一。

---

## 🔧 后续使用

### 添加新的签到目标

```bash
cd ~/telegram-auto-checkin
source venv/bin/activate
python add_target.py
```

### 查看所有群组名称

```bash
python list_groups.py
```

### 查看运行日志

```bash
# 查看实时日志
tail -f auto_checkin.log

# 或查看 systemd 日志
sudo journalctl -u telegram-auto-checkin -f
```

### 停止签到服务

```bash
# 如果用 systemd
sudo systemctl stop telegram-auto-checkin

# 如果用 run.sh
./stop.sh
```

### 重启签到服务

```bash
sudo systemctl restart telegram-auto-checkin
```

---

## 🐛 常见问题

### 问：安装时提示权限不足？

**答：** 在命令前加 `sudo`，例如：
```bash
sudo bash install.sh
```

### 问：提示找不到 git 命令？

**答：** 先安装 git：
```bash
sudo apt update
sudo apt install git -y
```

### 问：登录时一直收不到验证码？

**答：** 
1. 检查手机号格式是否正确（必须以 + 开头）
2. 确保手机能正常接收 Telegram 消息
3. 有些账号会通过 Telegram 应用内发送验证码，而不是短信

### 问：签到失败，提示找不到群组？

**答：** 
1. 运行 `python list_groups.py` 查看所有群组的精确名称
2. 使用精确名称重新配置，或使用群组 ID（数字）

### 问：如何查看签到是否成功？

**答：** 查看日志：
```bash
tail -30 auto_checkin.log
```

### 问：想更换签到时间怎么办？

**答：** 编辑 `.env` 文件：
```bash
nano .env
```
修改 `CHECKIN_HOUR` 和 `CHECKIN_MINUTE`，然后重启服务。

### 问：如何卸载？

**答：**
```bash
# 停止服务
sudo systemctl stop telegram-auto-checkin
sudo systemctl disable telegram-auto-checkin

# 删除服务文件
sudo rm /etc/systemd/system/telegram-auto-checkin.service

# 删除项目目录
rm -rf ~/telegram-auto-checkin
```

---

## 📞 需要帮助？

如果遇到问题：

1. 查看 [README.md](README.md) 完整文档
2. 查看 [CHECKLIST.md](CHECKLIST.md) 排查问题
3. 在 GitHub 提 Issue
4. 检查日志文件找到错误信息

---

## 🎉 部署成功！

恭喜你！现在机器人会在每天指定时间自动帮你签到了！

记得：
- ✅ 保持服务器运行
- ✅ 定期查看日志
- ✅ 如果长期不用，记得停止服务省资源

祝使用愉快！ 🎊
